{
    "name": "resize",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc && rm lambda.zip && zip -r lambda.zip package.json dist/* node_modules/"
    },
    "dependencies": {
        "aws-lambda": "^1.0.7",
        "aws-sdk": "^2.1157.0",
        "sharp": "^0.30.6",
        "typescript": "^4.7.4"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.100",
        "@types/sharp": "^0.30.4"
    }
}
