'use strict';

/**
 * manufacturer-type service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::manufacturer-type.manufacturer-type');
