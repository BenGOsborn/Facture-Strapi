'use strict';

/**
 * manufacturer-account service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::manufacturer-account.manufacturer-account');
