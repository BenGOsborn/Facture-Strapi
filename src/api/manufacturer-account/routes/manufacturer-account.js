'use strict';

/**
 * manufacturer-account router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::manufacturer-account.manufacturer-account');
